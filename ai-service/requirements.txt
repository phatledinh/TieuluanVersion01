# ── AI Service Dependencies — Chapter 3.9.1 ──

# Web Framework (FastAPI — PDF 3.9.1 / 4.1.1)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Deep Learning (PyTorch — PDF 3.4)
torch>=2.2,<3.0

# Knowledge Graph (Neo4j — PDF 3.5)
neo4j>=5.14.0

# Vector Database (FAISS — PDF 3.6.2)
faiss-cpu>=1.8.0

# Embeddings (SentenceTransformers — PDF 3.6.2)
sentence-transformers>=2.7.0

# LLM Provider (Groq — PDF 3.6.1)
groq>=0.5.0

# Data processing
pandas>=2.1.0
numpy>=1.24.0

# HTTP client (inter-service communication — PDF 4.5.1)
httpx>=0.27.0

# Settings
python-dotenv>=1.0.0
pydantic-settings>=2.0.0
